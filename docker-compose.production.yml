services:
  api:
    environment:
      APP_ENV: production
      CORS_ORIGINS: ${CORS_ORIGINS:-[]}
      ALLOWED_HOSTS: ${ALLOWED_HOSTS:?Set ALLOWED_HOSTS to a JSON list containing the public hostname}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}

  web:
    ports:
      - "127.0.0.1:${WEB_PORT:-8080}:80"
